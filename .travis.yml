language: java
jdk:
  - oraclejdk8
env:
 DOCKER_COMPOSE_VERSION: 1.8.0

services:
  - docker

script:
  - mvn package -Dspring.profiles.active=dev docker:build


before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose -f src/main/docker/app.yml

notifications:
  email:
    recipients:
      - grigoriy.didorenko@gmail.com
      - kotun13@mail.ru
      - atia29@mail.ru
    on_success: never
    on_failure: always
